# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database (Task #5)
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0

# Cache / broker (Task #3, #18)
redis>=5.0.0

# LLM / RAG (Tasks #15, #22)
openai>=1.30.0
langchain>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.2.0

# Vector store (Task #16)
pinecone-client>=3.2.0

# Workers (Task #18)
celery[redis]>=5.3.0

# Observability (Task #36)
prometheus-client>=0.20.0

# Experiment tracking (Task #29)
mlflow>=2.14.0

# Authentication (Task #9)
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4

# Document parsing (Task #13)
pypdf>=4.0.0
python-docx>=1.1.0

# HTTP client
httpx>=0.27.0
