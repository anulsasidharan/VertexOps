// VertexOps frontend shell — reserved for Task #31 (feature/frontend-shell-auth)
// Business logic will be added in Phase 6.

import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>VertexOps — coming soon</div>
  </React.StrictMode>
);
