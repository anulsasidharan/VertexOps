export function HomePage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-slate-400">
        VertexOps dashboard shell is ready. Documents, indexes, query, and evaluation
        pages are added in the next delivery tasks.
      </p>
    </div>
  );
}
