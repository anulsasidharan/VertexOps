# VertexOps Vite app (`frontend/`) — used for "Log in" / "Sign up" on the marketing site
NEXT_PUBLIC_VERTEXOPS_DASHBOARD_URL=http://localhost:5173
# Optional: where Next.js should proxy /api/* (local FastAPI). Default http://127.0.0.1:8000
# VERTEXOPS_API_PROXY_TARGET=http://127.0.0.1:8000
